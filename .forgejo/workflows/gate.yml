# SPDX-FileCopyrightText: 2026 ursa.nz <code@ursa.nz>
# SPDX-License-Identifier: GPL-3.0-or-later

# Build gate. On every PR and push to main: cross-compiles the onym-engine Rust core for the
# three packaged ABIs, runs ktlint + the :core unit tests, and assembles a debug-signed APK.
# No secrets, no release signing — that lives in release.yml, triggered by version tags.
name: gate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    # Pin the image explicitly: the runner is registered with ubuntu-latest -> node:22-bookworm,
    # but this build needs a full toolchain host. catthehacker's act image is already cached on
    # the runner, so this is a no-op pull.
    container:
      image: catthehacker/ubuntu:act-22.04
    env:
      ANDROID_SDK_ROOT: /opt/android-sdk
      ANDROID_HOME: /opt/android-sdk
      NDK_VERSION: "28.2.13676358"
      RUST_VERSION: "1.96.0"
      ENGINE_REF: main
    steps:
      - name: Check out Onymdroid
        uses: actions/checkout@v4

      - name: Check out onym-engine (latest)
        run: |
          set -eux
          git clone --depth 1 --branch "$ENGINE_REF" \
            https://forge.ursa.nz/ursa-nz/onym-engine.git onym-engine
          echo "engine_dir=$GITHUB_WORKSPACE/onym-engine" >> "$GITHUB_ENV"
          echo "onym-engine @ $(git -C onym-engine rev-parse HEAD)" >> "$GITHUB_STEP_SUMMARY"

      - name: Set up JDK 21
        uses: https://github.com/actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Cache Android SDK + NDK
        uses: https://github.com/actions/cache@v4
        with:
          path: /opt/android-sdk
          key: android-sdk-p36-bt36.1.0-ndk${{ env.NDK_VERSION }}

      - name: Install Android SDK + NDK
        run: |
          set -eux
          sdk="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin"
          if [ ! -x "$sdk/sdkmanager" ]; then
            mkdir -p "$ANDROID_SDK_ROOT/cmdline-tools"
            curl -sSLo /tmp/cmdtools.zip \
              https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
            unzip -q /tmp/cmdtools.zip -d /tmp/cmdtools
            mv /tmp/cmdtools/cmdline-tools "$ANDROID_SDK_ROOT/cmdline-tools/latest"
          fi
          export PATH="$sdk:$PATH"
          # `run:` steps execute under `bash -eo pipefail`; `yes` takes SIGPIPE (141) when
          # sdkmanager stops reading. Drop pipefail for just this pipe so the real exit status
          # is sdkmanager's, not the broken-pipe signal.
          ( set +o pipefail; yes | sdkmanager --licenses >/dev/null )
          sdkmanager --install "platform-tools" "platforms;android-36" \
            "build-tools;36.1.0" "ndk;$NDK_VERSION" >/dev/null
          echo "ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/$NDK_VERSION" >> "$GITHUB_ENV"

      - name: Cache Cargo + Gradle
        uses: https://github.com/actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.rustup/toolchains
            ~/.gradle/caches
            ~/.gradle/wrapper
            onym-engine/target
          key: build-${{ hashFiles('gradle/libs.versions.toml', 'onym-engine/Cargo.lock') }}
          restore-keys: build-

      - name: Install Rust ${{ env.RUST_VERSION }} + Android targets
        run: |
          set -eux
          if ! command -v rustup >/dev/null; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \
              | sh -s -- -y --default-toolchain none
            echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
            export PATH="$HOME/.cargo/bin:$PATH"
          fi
          rustup toolchain install "$RUST_VERSION" --profile minimal
          rustup target add --toolchain "$RUST_VERSION" \
            aarch64-linux-android armv7-linux-androideabi x86_64-linux-android

      - name: ktlint + tests + debug APK
        run: |
          ./gradlew --no-daemon -Donym.engine.dir="$engine_dir" \
            ktlintCheck :core:test :app:assembleDebug
